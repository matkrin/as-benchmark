{
  "name": "as-least-squares-benchmark",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node tests",
    "asbuild:untouched": "asc assembly/index.ts --target debug --exportRuntime",
    "asbuild:optimized": "asc assembly/index.ts --target release --exportRuntime --noAssert -O3",
    "asbuild": "npm run asbuild:untouched && npm run asbuild:optimized",
    "benchmark": "node benchmark"
  },
  "author": "matkrin",
  "license": "MIT",
  "dependencies": {
    "@assemblyscript/loader": "^0.19.23",
    "benchmark": "^2.1.4",
    "ml-matrix": "^6.9.0"
  },
  "devDependencies": {
    "assemblyscript": "^0.19.23"
  }
}
